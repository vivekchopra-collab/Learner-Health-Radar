# Copy this file to .env and fill in your own key:
#   cp .env.example .env
#
# Read server-side only (vite.config.ts / scripts), never bundled into the
# client. The real .env is gitignored — do not commit it.
ANTHROPIC_API_KEY=
